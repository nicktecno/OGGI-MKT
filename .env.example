# Opcional: variáveis para `docker compose` na raiz (substituição ${VAR} em docker-compose.yml).
# Copie para `.env` na raiz se quiser sobrescrever os padrões do compose.
#
# INTERNAL_API_SECRET=
# AUTH_SECRET=
# FRONTEND_URL=http://localhost:3000
